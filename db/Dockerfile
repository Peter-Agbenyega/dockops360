# Use lightweight MySQL-compatible server
FROM mariadb:latest

# Set environment variables for the database
ENV MYSQL_ROOT_PASSWORD=rootpassword
ENV MYSQL_DATABASE=dockopsdb
ENV MYSQL_USER=dockuser
ENV MYSQL_PASSWORD=dockpassword

# Expose the database port
EXPOSE 3306
